version: "2"

run:
  timeout: 3m

linters:
  default: standard
  enable:
    - errcheck
    - gosec
    - gocritic
    - misspell
    - revive
    - unconvert
    - unused

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
